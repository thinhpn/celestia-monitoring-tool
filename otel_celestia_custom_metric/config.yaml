receivers:  
  otlp:
    protocols:
      http:
        endpoint: 162.55.165.91:3456        
processors:
  batch:

exporters:
   prometheus:
     endpoint: 0.0.0.0:6789

service:
  pipelines:
    metrics:
      receivers: [otlp]
      processors: [batch]
      exporters: [prometheus]